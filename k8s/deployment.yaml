apiVersion: apps/v1
kind: Deployment
metadata:
  name: crudkub-server
  labels:
    app: crudkub-server
spec:
  replicas: 3
  selector:
    matchLabels:
      app: crudkub-server
  template:
    metadata:
      labels:
        app: crudkub-server
    spec:
      containers:
      - name: crudkub-server
        image: ndalton132/crudkub-server:latest
        ports:
        - containerPort: 3000
        envFrom:
        - secretRef:
            name: crudkub-secret
